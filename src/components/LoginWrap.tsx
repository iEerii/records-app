import Form from "./Form";
import { MainWrap, Title } from '../elements/FormElements'

const LoginWrap = () => {
    return (
        <MainWrap>
            <Title>Login</Title>
            <Form />
        </MainWrap>
    );
}

export default LoginWrap;